import { Image } from "@chakra-ui/react";
import image from "../../assets/images/icon-plus.svg"

const PlusIcon = () => {
  return <Image src={image} alt="plus icon" width={5} height={5} />;
};

export default PlusIcon;
